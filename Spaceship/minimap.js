
function RenderMinimap()
{
    /*
    var x      = Math.floor(canvas.width  * 0.75);
    var y      = Math.floor(canvas.height * 0.75);
    var width  = Math.floor(canvas.width  * 0.20);
    var height = Math.floor(canvas.height * 0.20);
    
    var factorX = 1.0 / MAP_WIDTH  * width;
    var factorY = 1.0 / MAP_HEIGHT * height;
    
    context.fillStyle = 'black';
    context.fillRect(x, y, width, height);
    
    context.strokeStyle = 'white';
    
    context.beginPath();
    
    var lx = x + player.x * factorX;
    var ly = y;
    
    context.moveTo(lx, ly);
    context.lineTo(lx, ly + height);
    
    lx = x;
    ly = y + player.y * factorY;
    
    context.moveTo(lx, ly);
    context.lineTo(lx + width, ly);
    
    context.stroke();
    */
}
